

<p> Add User as Student to Course


<%= form_tag( create_course_student_path , :method => "post") do %>
<%= select("CourseStudent", "course_id", @courses.collect {|course| [ course.name, course.id ] }, {:prompt => 'Select the Course'})
%>
<p>
<%= select("CourseStudent", "user_id", @users.collect {|user| [ user.name, user.id ] }, {:prompt => 'Select the User for Student'})
%>
<p>
  <%= submit_tag("On to Courses") %>
<% end %>

<!-- select("post", "person_id", Person.all.collect {|p| [ p.name, p.id ] }, {:include_blank => 'None'})
 -->

<!--       model   course_id   @courses.all.coll     course.
 -->
